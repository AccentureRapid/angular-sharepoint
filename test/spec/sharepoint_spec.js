describe('ExpertsInside.SharePoint', function () {
  'use strict';

  it('is a angular module', function () {
    var mod = module('ExpertsInside.SharePoint');
    inject(function(foo) { expect(foo).to.be.equal(1); });
  });
});
